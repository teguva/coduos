# Drop this file in /etc/nginx/conf.d/coduos.conf (or include it from nginx.conf).
# CoduOS writes server blocks under /var/lib/coduos/nginx/conf.d/.
# The dashboard daemon listens on 127.0.0.1:13209.

include /var/lib/coduos/nginx/conf.d/*.conf;
